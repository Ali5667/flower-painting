# flower-painting
from turtle import *
speed(0)
bgcolor('gray')
color ('aqua')
for i in range(160):
     rt(i)
     circle(150, i)
     rt(90)
hideturle()
done

from turtle import *
speed(0)
bgcolor('gray')
color ('aqua')
